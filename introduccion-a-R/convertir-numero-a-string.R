#Crear una función que convierta un número entero en cadena

#Se define la función personalizada con el parámetro n y se asigna a una variable
number_to_string <- function(n) {

#Se aplica la funcion as.character() que trasnforma elementos en string
  cadena <- as.character(n)
  print(cadena)
}

